# Messy Migration

This is a refactored modular Flask project submitted as part of the SDE Internship assignment for RetainSure.

## 🔧 Features

- Modular Flask architecture
- SQLite database setup with `init_db.py`
- API endpoints to:
  - List all users
  - Claim random points for a user
  - View claim history
- Swagger UI documentation using Flasgger
- HTML templates for user-friendly interaction
- Unit testing with `pytest`

## 📂 Project Structure

